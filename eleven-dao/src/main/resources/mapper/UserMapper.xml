<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fantasy.eleven.dao.UserDao">
    <sql id="field">id,gmt_create,gmt_modified,user_login_name,user_password,user_name</sql>
    <insert id="insert">
        INSERT users_list (id,gmt_create,gmt_modified,user_login_name,user_password,user_name) VALUES (#{id},#{gmtCreate},#{gmtModified},#{userLoginName},#{userPassword},#{userName})
    </insert>
    <delete id="delete">
        DELETE FROM users_list WHERE id=#{id}
    </delete>
    <update id="update">
        UPDATE users_list
        <trim prefix="SET" suffixOverrides=",">
            <if test="name!= null and name !='' and name != 'null'">name=#{name},</if>
            <if test="sex!= null ">sex=#{sex},</if>
            <if test="address!= null and address !='' and address != 'null'">address=#{address}</if>
        </trim>
        WHERE id=#{id} ;
    </update>
    <select id="select" resultType="UserModel">
        SELECT * FROM users_list
        <trim prefix="WHERE" prefixOverrides="AND">
            <if test="id!= null and id !='' ">id=#{id}</if>
        </trim>
    </select>
</mapper>